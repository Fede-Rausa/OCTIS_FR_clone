

### imposto la versione di python 3.10.1 con pyenv

pyenv install 3.10.1

pyenv local 3.10.1

python --version 

### creo l'ambiente virtuale e lo attivo da powershell-vscode

python -m venv OCTIS_py_env

Set-ExecutionPolicy -ExecutionPolicy Unrestricted -Scope CurrentUser

OCTIS_py_env/Scripts/activate.ps1

### installo le dipendenze
# attenzione: in requirements_dev.txt la voce click==7.0 è stata rimpiazzata con click==8.1.3


pip install -r requirements.txt
pip install -r requirements_dev.txt




## nella classe Preprocessing sostituisco:
        self.min_df = min_df
        self.max_df = max_df
con 
        self.min_df = float(min_df)
        self.max_df = float(max_df)


